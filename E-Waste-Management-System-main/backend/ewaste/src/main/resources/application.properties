spring.application.name=Smart e-Waste Collection
server.port=${PORT:8080}

# --- Database Configuration ---
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ecowaste_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:mahez@123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# --- JWT Configuration ---
app.jwt.secret=${JWT_SECRET:iJHZ0lKj+WcDd2zU4N7JfYhbqK1F1xvG4H7vjxV6eHg=}
app.jwt.expiration-ms=86400000

# --- Google Maps Configuration ---
google.maps.api.key=${GOOGLE_MAPS_API_KEY:AIzaSyBS-ezZgIJ6uHX10naFGM4KtUND8b0O5BI}

# --- Email Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:mahesh123qr@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ntwg nzhm aiwx jmeu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

# --- Logging ---
logging.level.org.springframework.security=DEBUG
spring.security.debug=true

# --- UPDATED FILE UPLOAD LIMITS ---
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=200MB

# Prevents connection reset for large files
server.tomcat.max-swallow-size=-1

# --- Additional Config ---
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
admin.id=${ADMIN_ID:1}